const library = require('./library.json');
class Book {
    static execute(UserInput) {
        if (UserInput === '/book') {
            // Aqui vem o código de execução do comando /book
        }
    }
}
module.exports = Book;